/**
 * Provides custom instructions for the Star Wars MCP server
 */

/**
 * Returns the custom instructions for the Peacock MCP server
 * @returns Custom instructions string
 */
export function getCustomInstructions(): string {
  return `

`;
}
